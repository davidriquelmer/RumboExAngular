import { ErroralertDirective } from './erroralert.directive';

describe('ErroralertDirective', () => {
  it('should create an instance', () => {
    const directive = new ErroralertDirective();
    expect(directive).toBeTruthy();
  });
});
